{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;800;900&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
:root {
  --mv-950:#2e0d52; --mv-800:#5b2b82; --mv-700:#7c3dac;
  --mv-600:#8f4cc9; --mv-500:#b39ddb; --mv-400:#c4b0e4;
  --mv-200:#e9d5ff; --mv-100:#f3e8ff; --mv-50:#faf5ff;
  --mv-glow: rgba(143,76,201,0.4);
}
*, *::before, *::after { box-sizing:border-box; }

/* ── Shell ── */
.salaire-edit-shell {
  font-family:'Inter',sans-serif;
  background: radial-gradient(ellipse at 20% 0%, #f3e8ff 0%, #ffffff 50%, #faf5ff 100%);
  min-height:90vh; padding:44px 20px; color:#0f172a;
  position:relative; overflow:hidden;
}
.salaire-edit-shell::before {
  content:''; position:fixed; top:-180px; right:-180px;
  width:560px; height:560px; border-radius:50%;
  background: radial-gradient(circle, rgba(143,76,201,.16), transparent 68%);
  pointer-events:none; z-index:0; animation:orbFloat1 9s ease-in-out infinite;
}
.salaire-edit-shell::after {
  content:''; position:fixed; bottom:-120px; left:-120px;
  width:420px; height:420px; border-radius:50%;
  background: radial-gradient(circle, rgba(179,157,219,.14), transparent 68%);
  pointer-events:none; z-index:0; animation:orbFloat2 11s ease-in-out infinite;
}
.salaire-edit-wrap { max-width:1000px; margin:0 auto; position:relative; z-index:1; }

/* ── Header ── */
.salaire-edit-head {
  display:flex; justify-content:space-between; align-items:flex-end;
  gap:18px; flex-wrap:wrap; margin-bottom:28px;
  animation:slideDown .7s cubic-bezier(.16,1,.3,1) both;
}
.salaire-edit-title h1 {
  font-family:'Outfit'; font-weight:900; font-size:2.4rem; margin:0;
  background: linear-gradient(135deg, var(--mv-950), var(--mv-600));
  -webkit-background-clip:text; -webkit-text-fill-color:transparent;
  display:flex; align-items:center; gap:14px;
}
.salaire-edit-title h1 i { font-size:2rem; -webkit-text-fill-color:var(--mv-600); }
.salaire-edit-title p { margin:8px 0 0; color:#64748b; font-size:1.02rem; font-weight:500; }

/* Back button */
.btn-back-salaire-edit {
  display:inline-flex; align-items:center; gap:9px; padding:11px 22px;
  border-radius:999px; border:1.5px solid rgba(179,157,219,.35);
  background:rgba(255,255,255,.88); color:var(--mv-700);
  font-weight:800; font-size:.93rem; cursor:pointer;
  transition:all .2s; backdrop-filter:blur(10px); text-decoration:none;
}
.btn-back-salaire-edit:hover { background:#fff; border-color:var(--mv-500); transform:translateY(-2px); box-shadow:0 8px 20px rgba(143,76,201,.16); color:var(--mv-700); }

/* Glass card */
.glass-card {
  background:rgba(255,255,255,.87);
  backdrop-filter:blur(22px); -webkit-backdrop-filter:blur(22px);
  border:1.5px solid rgba(179,157,219,.28);
  border-radius:24px;
  box-shadow:0 10px 40px rgba(143,76,201,.10);
  padding:32px 36px;
  animation:slideUp .7s cubic-bezier(.16,1,.3,1) .2s both;
}

/* Employee box */
.employee-box-salaire {
  padding:16px 20px; border-radius:14px;
  background:linear-gradient(135deg,rgba(143,76,201,.08),rgba(179,157,219,.05));
  border:1.5px solid rgba(179,157,219,.28);
  color:var(--mv-700); font-weight:800; font-size:1.05rem;
  display:flex; justify-content:space-between; align-items:center;
}

/* Form elements */
.form-label-salaire-edit {
  display:block; margin-bottom:8px; font-weight:800; font-size:.88rem;
  color:var(--mv-700); text-transform:uppercase; letter-spacing:.06em;
}
.form-label-salaire-edit i { margin-right:6px; color:var(--mv-600); }

.form-control-salaire-edit {
  width:100%; padding:12px 16px; border-radius:14px;
  border:1.5px solid rgba(179,157,219,.28); background:var(--mv-50);
  font-family:inherit; font-size:.95rem; color:#0f172a;
  outline:none; transition:all .2s;
}
.form-control-salaire-edit:focus { border-color:var(--mv-600); box-shadow:0 0 0 4px rgba(143,76,201,.14); background:#fff; }

.input-group-modern {
  display:flex; align-items:stretch;
}
.input-group-modern .form-control-salaire-edit {
  border-top-right-radius:0; border-bottom-right-radius:0;
}
.input-group-text-modern {
  padding:12px 16px; border-radius:0 14px 14px 0;
  border:1.5px solid rgba(179,157,219,.28); border-left:none;
  background:linear-gradient(135deg,rgba(143,76,201,.08),rgba(179,157,219,.05));
  color:var(--mv-700); font-weight:800; font-size:.9rem;
}

.conversion-info {
  margin-top:8px; padding:8px 12px; border-radius:10px;
  background:linear-gradient(135deg,rgba(143,76,201,.08),rgba(179,157,219,.05));
  color:var(--mv-700); font-size:.85rem; font-weight:700;
}

/* Buttons */
.btn-save-salaire-edit {
  display:inline-flex; align-items:center; gap:9px; padding:13px 26px;
  border-radius:999px; border:none;
  background:linear-gradient(135deg,var(--mv-700),var(--mv-600));
  color:#fff; font-weight:900; font-size:.95rem; cursor:pointer;
  transition:all .2s; box-shadow:0 8px 20px var(--mv-glow);
  font-family:'Outfit';
}
.btn-save-salaire-edit:hover { transform:translateY(-2px); box-shadow:0 12px 26px var(--mv-glow); }

.btn-cancel-salaire-edit {
  display:inline-flex; align-items:center; gap:9px; padding:13px 26px;
  border-radius:999px; border:1.5px solid rgba(179,157,219,.35);
  background:rgba(255,255,255,.88); color:var(--mv-700);
  font-weight:800; font-size:.95rem; cursor:pointer;
  transition:all .2s; text-decoration:none;
}
.btn-cancel-salaire-edit:hover { background:#fff; border-color:var(--mv-500); transform:translateY(-2px); color:var(--mv-700); }

/* Alert */
.alert-modern {
  padding:16px 20px; border-radius:16px; margin-bottom:20px;
  background:#fff1f2; border:1.5px solid #fecdd3; color:#be123c;
  font-weight:700; display:flex; align-items:center; gap:10px;
}

/* Animations */
@keyframes slideDown { from{opacity:0;transform:translateY(-18px);}to{opacity:1;transform:translateY(0);} }
@keyframes slideUp   { from{opacity:0;transform:translateY(20px);}to{opacity:1;transform:translateY(0);} }
@keyframes orbFloat1 { 0%,100%{transform:translate(0,0) scale(1);}50%{transform:translate(-28px,20px) scale(1.05);} }
@keyframes orbFloat2 { 0%,100%{transform:translate(0,0) scale(1);}50%{transform:translate(20px,-26px) scale(1.07);} }

@media(max-width:768px){
  .salaire-edit-head { flex-direction:column; align-items:flex-start; }
  .glass-card { padding:24px 20px; }
}
</style>
{% endblock %}

{% block body %}
<div class="salaire-edit-shell">
  <div class="salaire-edit-wrap">

    <!-- 🔙 BACK BUTTON & HEADER -->
    <div class="salaire-edit-head">
      <div>
        <a href="{{ path('app_rh_salaire_index') }}" class="btn-back-salaire-edit">
          <i class="fas fa-arrow-left"></i> Retour aux Salaires
        </a>
        <div class="salaire-edit-title" style="margin-top:16px;">
          <h1><i class="fas fa-edit"></i> Modifier le Salaire</h1>
          <p>Mettre à jour les informations du salaire d'employé</p>
        </div>
      </div>
    </div>

    <!-- FORM ERRORS -->
    {% if form_errors(form) %}
        <div class="alert-modern">
            <i class="fas fa-exclamation-triangle"></i>
            {{ form_errors(form) }}
        </div>
    {% endif %}

    <!-- 💎 FORM CARD -->
    <div class="glass-card">

        <!-- 👤 EMPLOYEE (LOCKED) -->
        <div class="mb-4">
            <label class="form-label-salaire-edit">
                <i class="fas fa-user-circle"></i> Employé
            </label>

            <div class="employee-box-salaire">
                {{ salaire.contract.employe.user.fullName }}
                <span class="badge bg-primary">Salaire #{{ salaire.id }}</span>
            </div>
        </div>

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <div class="row g-4">

            <!-- MOIS -->
            <div class="col-md-6">
                <label class="form-label-salaire-edit">
                    <i class="fas fa-calendar-alt"></i> Mois
                </label>
                {{ form_widget(form.mois, {'attr': {'class': 'form-control-salaire-edit'}}) }}
                <div class="text-danger small mt-1">
                    {{ form_errors(form.mois) }}
                </div>
            </div>

            <!-- ANNEE -->
            <div class="col-md-6">
                <label class="form-label-salaire-edit">
                    <i class="fas fa-calendar"></i> Année
                </label>
                {{ form_widget(form.annee, {'attr': {'class': 'form-control-salaire-edit'}}) }}
                <div class="text-danger small mt-1">
                    {{ form_errors(form.annee) }}
                </div>
            </div>

            <!-- MONTANT -->
            <div class="col-md-6">
                <label class="form-label-salaire-edit">
                    <i class="fas fa-coins"></i> Montant
                </label>

                <div class="input-group-modern">
                    {{ form_widget(form.montant, {'attr': {'class': 'form-control-salaire-edit'}}) }}
                    <span class="input-group-text-modern">{{ currency }}</span>
                </div>

                <div class="text-danger small mt-1">
                    {{ form_errors(form.montant) }}
                </div>

                {% if currency != 'TND' %}
                    <div id="conversionInfo" class="conversion-info">
                        💡 Conversion en cours...
                    </div>
                {% endif %}
            </div>

            <!-- STATUT -->
            <div class="col-md-6">
                <label class="form-label-salaire-edit">
                    <i class="fas fa-info-circle"></i> Statut
                </label>
                {{ form_widget(form.statut, {'attr': {'class': 'form-control-salaire-edit'}}) }}
                <div class="text-danger small mt-1">
                    {{ form_errors(form.statut) }}
                </div>
            </div>

            <!-- DATE -->
            <div class="col-md-12">
                <label class="form-label-salaire-edit">
                    <i class="fas fa-calendar-check"></i> Date de paiement
                </label>
                {{ form_widget(form.date_paiement, {'attr': {'class': 'form-control-salaire-edit'}}) }}
                <div class="text-danger small mt-1">
                    {{ form_errors(form.date_paiement) }}
                </div>
            </div>

        </div>

        <!-- 🔥 BUTTONS -->
        <div class="d-flex justify-content-end gap-3 mt-4">
            <a href="{{ path('app_rh_salaire_index') }}" class="btn-cancel-salaire-edit">
                <i class="fas fa-times-circle"></i> Annuler
            </a>
            <button type="submit" class="btn-save-salaire-edit">
                <i class="fas fa-save"></i> Mettre à jour
            </button>
        </div>

        {{ form_end(form) }}

    </div>

  </div>
</div>

<script>
const montantInput = document.querySelector('[name$="[montant]"]');

const rate = {{ rate }};
const currency = "{{ currency }}";

if (montantInput && currency !== "TND") {
    function updateConversion() {
        let value = parseFloat(montantInput.value);
        if (!isNaN(value)) {
            let tnd = (value / rate).toFixed(2);
            document.getElementById("conversionInfo").innerText = `💡 ≈ ${tnd} TND`;
        } else {
            document.getElementById("conversionInfo").innerText = "💡 Conversion en cours...";
        }
    }

    montantInput.addEventListener("input", updateConversion);
    // Show conversion on page load
    updateConversion();
}

// Validation helpers
function showError(input, message) {
    let error = input.parentElement.querySelector('.live-error');
    if (!error) {
        error = document.createElement('div');
        error.className = 'text-danger small live-error';
        input.parentElement.appendChild(error);
    }
    error.innerText = message;
}

function clearError(input) {
    const error = input.parentElement.querySelector('.live-error');
    if (error) error.remove();
}

// Input references
const montant = document.querySelector('[name$="[montant]"]');
const annee = document.querySelector('[name$="[annee]"]');
const mois = document.querySelector('[name$="[mois]"]');
const statut = document.querySelector('[name$="[statut]"]');

// Real-time validation
montant.addEventListener('input', () => {
    clearError(montant);
    if (montant.value && parseFloat(montant.value) <= 0) {
        showError(montant, "Montant doit être positif");
    }
});

annee.addEventListener('input', () => {
    clearError(annee);
    if (annee.value && (annee.value < 2000 || annee.value > 2100)) {
        showError(annee, "Année invalide");
    }
});

mois.addEventListener('change', () => {
    clearError(mois);
});

statut.addEventListener('change', () => {
    clearError(statut);
});

// Final submit validation
document.querySelector('form').addEventListener('submit', function(e) {
    let hasError = false;
    document.querySelectorAll('.live-error').forEach(el => el.remove());

    if (!montant.value || parseFloat(montant.value) <= 0) {
        showError(montant, "Montant invalide");
        hasError = true;
    }

    if (!annee.value || annee.value < 2000 || annee.value > 2100) {
        showError(annee, "Année invalide");
        hasError = true;
    }

    if (!mois.value) {
        showError(mois, "Mois obligatoire");
        hasError = true;
    }

    if (!statut.value) {
        showError(statut, "Statut obligatoire");
        hasError = true;
    }

    if (hasError) {
        e.preventDefault();
    }
});
</script>

{% endblock %}